# 状态中心设计需求

面向 Wuu-Tavern（基于 OpenTavern 的通用前端）。本文只描述平台能力，不绑定任何角色卡、世界书或具体剧本。

状态中心是独立于聊天气泡的账本。对话里讲故事，账本里记事实。换一张角色卡也应能用。

---

## 1. 要解决什么问题

长对话里模型只看见最近若干条消息，三件事会反复坏：

1. **时间对不上。** 说过「三天后见面」，过了两轮又写成「五天后」。用户无法从界面确认「现在是第几天、还剩几天」。
2. **做过的事被忘掉或重写。** 已经发生的约定、冲突、完成项，下一轮被当成没发生，或日期错位。
3. **人物关系被重置。** 某人对用户的态度、服从、未完成要求，下次见面又回到初见。用户只能靠翻聊天记录，模型则经常不翻。

常见的错误做法：

- 把整张表打印在每条回复末尾：对话不可读，上下文被表格占满，换卡还要改提示词格式。
- 只写在作者注释或世界书里、没有独立界面：用户看不见、不能点开某一张表、不能确认模型读的是不是最新的。
- 在页面脚本里按某个剧本的人名做特殊判断：换卡即失效，也把平台和内容绑死。

状态中心要同时服务两方：

- **用户**：点开就能看到「现在记了哪些表、每张表最新内容」。
- **模型**：每次生成前读到相关表，回复正文里不再复述表。

---

## 2. 设计原则

1. **平台与内容分离。** 仓库和页面功能不出现任何剧本人名、地名、设定。表名、列名由角色卡或模型约定，平台只认「表」这种结构。
2. **账本与正文分离。** 聊天区只保留叙事。表只存在于状态中心。
3. **先读后写。** 生成前注入只读账本；生成后若有更新，写入状态中心，不留在气泡里。
4. **能换卡。** 同一套 UI 和同一套读写协议，换角色卡、换世界书不改平台代码。
5. **不卡死页面。** 禁止对整页 DOM 做高频改写。OpenTavern 主逻辑在 IIFE 内，不能假设 `formatMessage` 等函数是全局可包装的。
6. **用户可检查。** 每张表可点开看全文。没有表时明确显示空状态，不要用假数据填。

---

## 3. 信息架构

每张角色卡（或每场对话）最多 **50** 张表。

每张表：

| 字段 | 说明 |
| --- | --- |
| `name` | 短名，例如「时间表」「完成表」「状态表」，由内容侧命名，平台不写死业务含义 |
| `content` | 纯文本，建议一行一条；平台不解析列语义 |
| `updated` | 最后写入时间 |

推荐（不是强制）三张用途不同的表，方便角色卡作者对齐：

### 3.1 时间表

回答「现在何时、今天已发生什么、未到期的约定还余多久」。

- 用「第 N 日 + 时段」这类相对时间，不要在正文里写死「五天后」却不递减。
- 一行一天或一个时段即可。

### 3.2 完成表

回答「哪一天做完了哪件事、结果如何」。

- 只追加已发生项，不删历史。
- 与时间表分开，避免「日程」和「档案」混成一段。

### 3.3 状态表

回答「每个相关人物现在怎样」。

- **一行一个人**，行数不限。
- 次要人物一行短记（见过几次、印象）。
- 主线或已发生过关系变化的人多记几列：进度、服从、着装/身体变化、现行要求、旧要求是否仍有效、下次正式接触、对用户的称呼。
- **不写用户本人。** 用户不是被追踪对象。
- 见过的人只改这一行，不从桌上消失。
- 「下次见面是否执行旧要求」看服从度和用户这一轮的命令，不写死「见面先执行旧令」。

角色卡可以不用这三张名字，也可以再加表（物品、地点、势力）。平台按 `name` 列出，不做剧本逻辑。

---

## 4. 界面

- 页面右下角常驻「状态中心」按钮，不挡住输入框。
- 点击打开模态框：左侧表名列表，右侧当前表全文。
- 点某一张表只切换右侧内容，不刷新整页、不打断输入。
- 空态文案：「还没有表。模型更新后出现在这里。」
- 表内容只在这里维护。不要在聊天气泡、作者注释栏、页面底部再镜像一份。

---

## 5. 与模型的协议

### 5.1 生成前：只读注入

在发出聊天请求前，把当前对话（或当前卡）下的表注入为一条系统消息，例如：

```
【状态中心·只读】禁止在正文、选项里复述或打印下列表。
<StatusTable name="时间表">
...
</StatusTable>
```

要求：

- 只注入「与本轮可能有关」的表；若无法判断相关性，宁可注入全部已有表，但总长度截断（建议上限约 8KB）。
- 注入内容对用户不可见。
- 不要把表再拼进用户气泡。

相关性（可选，后做）：按本轮用户句、当前出场名、地点关键词，从各表中抽相关行，而不是每次塞 50 张全文。

### 5.2 生成后：隐藏更新

模型若要改表，只允许写在回复最末的隐藏标签里：

```
<StatusTable name="时间表">整表或覆盖后的全文</StatusTable>
<StatusTable name="完成表">...</StatusTable>
<StatusTable name="状态表">...</StatusTable>
```

平台行为：

1. 按 `name` 覆盖写入状态中心（同名更新，无名则新建，超过 50 张丢掉最旧）。
2. 从准备展示的正文、以及已存入的助手消息里删除这些标签及其纯文本抄本。
3. 用户看到的气泡里不得出现表标题、管道表、或「第 N 日 | 今日已发生」这类账本行。

允许识别的标记（按优先级）：

- `<StatusTable name="...">` / `<OTTable>` / `<Ledger>`
- 纯文本块以「时间表 / 完成表 / 状态表」单独成行起头时，仅作为兼容，仍应藏掉并入库

禁止把下列内容写入「状态表」：

- 用户本人一行
- 角色卡里的玩家 HUD（例如以「状态：」开头、记录玩家资源/位置的那一行）
- 时间表行（「第 N 日 | 今日已发生 | 预约」）

HUD 若出现在正文里，应删除或忽略，不要当成人物状态。

### 5.3 正文禁令（写给角色卡作者，平台用正则兜底）

角色卡系统提示应写明：

- 正文、选项、可见状态栏都不打印整表。
- 不要用「第 N 日 | …」把账本抄进故事。
- 更新只用文末隐藏标签。

平台不能指望模型每次都听话，所以展示层必须再剥一层。但展示层只按「通用账本行」剥，不按人名单剥。

---

## 6. 实现约束（给改代码的人）

1. OpenTavern 主脚本是 IIFE，`formatMessage`、`finishAssistantMessage`、`getActiveConv` **不是**可靠的全局钩子。不要把功能建立在包装这些函数上。
2. 拦截 `window.fetch` 注入只读表是可行路径之一；同时要能处理非 fetch 的发送方式（若后续出现）。
3. 若用 DOM 观察聊天区来藏表：
   - 只观察消息容器（如 `#messagesContainer`），不要观察整个 `document.body`
   - 改节点前加锁，改完打标记，避免观察器自己触发自己导致页面卡死
   - 不要在每次变动时无节流地 `persistState`
4. 表数据优先挂在「当前对话」上，导出备份时应能一起带走。不要写死在全局一份、换卡还串数据。
5. 脚本必须能通过语法检查。正则里不要把换行写进字符类导致源码断行。
6. 平台代码、注释、README 片段都不要出现具体剧本名、角色名。

---

## 7. 验收

换任意一张角色卡，走一轮对话，应满足：

- [ ] 右下角能打开状态中心，列表和详情可点，页面不卡
- [ ] 模型回复里的隐藏标签或账本行不留在气泡中
- [ ] 打开状态中心能看到本次写入的表，点表名能看到对应正文
- [ ] 状态表没有用户本人、没有玩家 HUD
- [ ] 下一轮请求的 payload 里能看到只读表（生成前已注入）
- [ ] 再导入另一张完全不同的卡，状态中心仍能建表、藏表、读表，无需改平台代码
- [ ] 导入卡、点开卡、进对话，页面可点击，刷新后不假死

不验收任何具体剧情是否「写得对」。剧情对错由角色卡和模型负责；平台只保证账本的存、读、藏、可见。

---

## 8. 明确不做

- 不为某个剧本预置表内容或人名规则
- 不在聊天区底部再做一条状态栏
- 不把表写进世界书条目来「代替」状态中心（角色卡可以提规则，数据仍在状态中心）
- 不用观察整个文档的方式「顺便」改卡片库、设置页
- 不在本仓库里存放角色卡、世界书、对话记录
